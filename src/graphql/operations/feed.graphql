query Feed($first: Int!) {
  feed(first: $first) {
    edges {
      cursor
      node {
        id
        content
        likeCount
        commentCount
        author {
          username
        }
      }
    }
    hasNextPage
  }
}
