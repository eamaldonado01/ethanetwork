query Feed($first: Int = 20, $after: ID) {
  feed(first: $first, after: $after) {
    edges {
      cursor
      node {
        ...PostFragment   # now carries imageUrl
      }
    }
    hasNextPage
  }
}

# make sure the fragment is pulled in
