mutation CommentOnPost($postId: ID!, $body: String!) {
  commentOnPost(postId: $postId, body: $body) {
    id
    body
    createdAt
    author {
      id
      username
      name
      imageUrl
    }
  }
}
