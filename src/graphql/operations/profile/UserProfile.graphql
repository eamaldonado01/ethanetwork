# import ../../fragments/PostFragment.graphql

query UserProfile($username: String!) {
  user(username: $username) {
    id
    username
    name
    bio
    website
    imageUrl
    github
    followersCount
    followingCount
    isFollowing

    posts { ...PostFragment }
  }
}
